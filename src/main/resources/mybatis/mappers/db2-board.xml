<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baron.member.sqlModel">

	<!-- Board 관련 쿼리 -->



	<select id="selectBoard" resultType="BoardModel">
		SELECT *
		FROM TESTLIB.BOOKBOARD
		WHERE boardtype='0'
		ORDER
		BY REGISDATE
	</select>

	<select id="selectBoardregister" resultType="BoardModel">
		SELECT *
		FROM
		TESTLIB.BOOKBOARD
		WHERE boardtype='0'
		ORDER BY REGISDATE
	</select>

	<select id="selectMaxGrade" resultType="int">
		SELECT MAX(grade)
		FROM
		BOOKMST
	</select>

	<select id="selectBoardtitle" resultType="BoardModel">
		SELECT *
		FROM
		TESTLIB.BOOKBOARD
		WHERE boardtype='0'
		ORDER BY title
	</select>

	<select id="selectBoardnum" parameterType="String" resultType="BoardModel">
		SELECT *
		FROM
		TESTLIB.BOOKBOARD
		WHERE boardnum=#{boardnum}

	</select>
	<insert id="insertBoard" parameterType="BoardModel">
		INSERT INTO TESTLIB.BOOKBOARD
		(title,content,REGISDATE,modifidate,id)
		VALUES
		(#{title},#{content},CURRENT DATE,CURRENT DATE,#{id})
	</insert>

	<delete id="deleteBoard" parameterType="String">
		DELETE FROM TESTLIB.BOOKBOARD
		WHERE
		boardnum=#{boardnum}
	</delete>

	<update id="modifyBoard" parameterType="BoardModel">
		UPDATE TESTLIB.BOOKBOARD
		SET
		content=#{content}, title=#{title}, modifidate=CURRENT DATE
		WHERE
		boardnum=#{boardnum}
	</update>

	<select id="noticeList" resultType="BoardModel">
		SELECT *
		FROM
		TESTLIB.BOOKBOARD
		WHERE
		boardtype='1'
		ORDER BY
		REGISDATE desc
	</select>

	<insert id="insertNotice" parameterType="BoardModel">
		INSERT INTO TESTLIB.BOOKBOARD
		(content, REGISDATE)
		VALUES
		(#{content}, CURRENT DATE)
	</insert>
	<delete id="deleteNotice" parameterType="String">
		DELETE FROM TESTLIB.BOOKBOARD
		WHERE
		boardnum=#{boardnum}
	</delete>

	<update id="modifyNotice" parameterType="BoardModel">
		UPDATE TESTLIB.BOOKBOARD
		SET
		content=#{content}, MODIFYDATE=CURRENT DATE
		WHERE boardnum=#{boardnum}
	</update>







</mapper>